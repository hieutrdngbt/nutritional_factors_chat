---
description: Run all tests (backend and frontend)
---

Run the complete test suite for the project:

1. Identify the backend and frontend directories
2. Run backend tests using `yarn test` in the backend directory
3. Run frontend tests using `yarn test` in the frontend directory
4. Analyze the test results and report:
   - Total tests run
   - Tests passed/failed
   - Test coverage metrics
   - Any failing tests with details
5. If any tests fail, provide suggestions for fixing them

Provide a comprehensive summary of the test results.
